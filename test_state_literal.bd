# test_state_literal.bd - THROWAWAY test, round 2: minimal 2-key
# multi-line object literal, to isolate whether multi-line literals
# work at all or something specific to the bigger one broke it.

var small = {
    "a": 1,
    "b": 2
}
log("Small literal OK: " + str(small))
